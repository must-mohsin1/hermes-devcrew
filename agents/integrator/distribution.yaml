# hermes-devcrew :: integrator
# A Hermes profile distribution. Install with:
#   hermes profile install ./agents/integrator --yes
name: devcrew-integrator
version: 0.2.0
description: "Integrator/synthesizer — merges verified work into one coherent deliverable."
hermes_requires: ">=0.12.0"
author: "hermes-devcrew"
license: "MIT"
env_requires:
  - name: OPENROUTER_API_KEY
    description: "OpenRouter API key — powers this agent's model"
    required: true
distribution_owned:
  - SOUL.md
  - config.yaml
  - skills
