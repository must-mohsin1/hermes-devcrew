# hermes-devcrew :: backend-dev
# A Hermes profile distribution. Install with:
#   hermes profile install ./agents/backend-dev --yes
name: devcrew-backend-dev
version: 0.2.0
description: "Backend implementation specialist — APIs, data layer, services, and tests."
hermes_requires: ">=0.12.0"
author: "hermes-devcrew"
license: "MIT"
env_requires:
  - name: OPENROUTER_API_KEY
    description: "OpenRouter API key — powers this agent's model"
    required: true
distribution_owned:
  - SOUL.md
  - config.yaml
  - skills
